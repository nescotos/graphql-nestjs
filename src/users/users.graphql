type Query{
    getUsers: [User]
}

type Mutation {
    createUser(createUserInput: CreateUserInput): User
}

type User {
    id: Int
    name: String
    username: String
    posts: [Post]
}

input CreateUserInput {
    name: String
    username: String
}